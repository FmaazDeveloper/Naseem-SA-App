import 'package:flutter/material.dart';

class touristPlaces extends StatelessWidget {
  const touristPlaces({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Tourist Places");
  }
}
